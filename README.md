# Facebook Page Insights Dashboard

A React app for viewing Facebook Page analytics and insights.

## Setup

1. Clone the repo
2. Run `npm install`
3. Copy `.env.example` to `.env` and add your Facebook App ID
4. Run `npm run dev`

## Tech Stack

- React + Vite
- Facebook JavaScript SDK
- Facebook Graph API
